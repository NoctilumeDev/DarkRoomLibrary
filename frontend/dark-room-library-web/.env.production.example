# Keep the relative default for same-origin deployment behind a reverse proxy.
# Use a full URL only when the frontend and API are deployed on different origins.
VITE_API_BASE_URL=/api/dark-room-library/v1
