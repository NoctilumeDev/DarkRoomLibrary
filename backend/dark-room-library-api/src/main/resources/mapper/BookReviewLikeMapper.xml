<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.darkroomlibrary.mapper.BookReviewLikeMapper">

    <select id="countByReviewIdAndUserId" resultType="integer">
        SELECT COUNT(*)
        FROM book_review_like
        WHERE review_id = #{reviewId}
          AND user_id = #{userId}
    </select>

    <select id="summarizeByReviewIds"
            resultType="org.darkroomlibrary.web.view.InteractionSummary">
        SELECT review_id AS reviewId,
               COUNT(*) AS totalCount,
               SUM(CASE WHEN user_id = #{userId} THEN 1 ELSE 0 END) AS viewerCount
        FROM book_review_like
        WHERE review_id IN
        <foreach collection="reviewIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        GROUP BY review_id
    </select>

    <delete id="deleteByReviewIdAndUserId">
        DELETE FROM book_review_like
        WHERE review_id = #{reviewId}
          AND user_id = #{userId}
    </delete>

    <delete id="deleteByReviewIds">
        DELETE FROM book_review_like
        WHERE review_id IN
        <foreach collection="reviewIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
