# 验证报告

## 2026-08-08 v1.2.5 安全补丁

本轮只将 `org.jsoup:jsoup` 从 `1.22.2` 升级到修复 `GHSA-pmhh-3w7g-xqp8` 的 `1.23.1`。Dependabot PR #9 的 CI 与 Security 均通过，合入 `main` 后同一提交的 CI、Security 和 Pages 再次通过，GitHub 安全告警状态已变为 `fixed`。

该补丁没有修改业务逻辑、数据库结构、API 或前端行为，因此不把 `v1.2.4` 的单实例与浏览器结果、`v1.2.3` 的三实例结果伪装为本轮重跑。发布标签仍会在目标提交上重新执行完整 CI，并要求同一提交的 Security 工作流成功；机器可读数字继续由 [验证基线](../.github/verification-baseline.json) 维护。

## 2026-08-08 v1.2.4 补丁验收

本轮针对 `v1.2.4` 的依赖安全、开源交付和本机直跑边界重新验收。后端业务与数据库结构未变；三实例结果不伪装为本轮重跑，继续保留在下方 `v1.2.3` 冻结基线中。

| 检查 | 2026-08-08 结果 |
| --- | --- |
| 后端 | `282/282`，失败、错误、跳过均为 0；JaCoCo 原始值 `3812/5277 = 72.238%`，70% 门禁通过 |
| 前端 | `68/68`；关键逻辑行覆盖率 `71.47%`；ESLint、生产构建、Demo 构建通过 |
| 依赖安全 | 新发现的 `nanoid < 3.3.17` 高危公告已通过锁定 `3.3.18` 修复，`npm audit --audit-level=high` 为 0 |
| 六身份链路 | 6 个固定身份真实登录，读者借还、管理员权限边界、采购认领/分配/物流/入库完成，共 70 次页面 API 响应 |
| 中间件控制变量 | Redis 和 RabbitMQ 分别验证正常连接与错误端口；不可用时健康状态为 `DEGRADED`，登录和数据库查询继续成功 |
| 并发一致性 | 20 场景、393 次请求；单库存、重复借还/预约、取消竞争、采购认领、32 次幂等入库和陈旧编辑均通过 |
| 浏览器与 F12 | 116 路由、456 次 API、6,114 次网络响应；Console、页面、请求、业务错误和横向溢出均为 0 |
| 资源边界 | Docker Desktop + 单个 MySQL 容器只剩 3.50 GiB 可用，未继续启动 JVM；改用已有本机 MySQL/Redis/RabbitMQ 后按控制变量完成单实例验收 |

本轮发现并修复了一个真实浏览器问题：`.env.development` 曾将 API 配成绝对 `localhost:20606`，绕过已存在的 Vite `/api` 代理并被严格 CSP 拦截。现统一使用同源 `/api/dark-room-library/v1`，不通过放宽 CSP 解决开发环境问题。机器可读结果见 [验证基线](../.github/verification-baseline.json)。

## 2026-08-02 v1.2.3 最终验收

### 1. 验证环境

| 组件 | 最终环境 |
| --- | --- |
| 后端 | JDK 17、Spring Boot 3.5.16、三个验收实例 `20606` / `20607` / `20608`，生产 Profile 实例 `20609` |
| 前端 | Vue 3.5.40、Vite 8.1.5、Nginx 生产构建端口 `5175` |
| 数据库 | MySQL 8.4、隔离 Compose 数据库 `dark_room_library_e2e` |
| 中间件 | Redis 7.4、RabbitMQ 4.2.3 |
| 浏览器 | Microsoft Edge / Playwright |

多人并发脚本为每个虚拟用户分配独立的保留测试网段 IP，保持源码默认限流配置不变，避免把 20 个模拟客户端错误合并为同一个 `127.0.0.1` 攻击源。

本轮使用隔离的 `drl-acceptance` 基础设施运行 MySQL 8.4、Redis 7.4 和 RabbitMQ 4.2.3，并串行启动三个 Spring Boot 验收实例、一个生产 Profile 实例、Nginx 前端和本地 Webhook 接收器。九个容器重启次数均为 0，基础设施健康检查通过；后端仅保留未配置公开 SMTP 时预期的通知补偿警告。

Windows 宿主机曾在其他中断批次记录 TCP 动态端口分配失败；中断结果未并入下方成功基线。对应证据、只读取证命令、串行测试顺序和安全恢复边界统一维护在 [`local-test-network-boundary.md`](local-test-network-boundary.md)。

### 2. 自动测试与构建

| 检查 | 结果 |
| --- | --- |
| 后端测试 | `282/282` 通过 |
| 前端测试 | `68/68` 通过 |
| 行覆盖率 | 后端模块 `72.23%`，前端关键逻辑范围 `71.47%`，两项 70% 门禁通过 |
| ESLint | 通过 |
| Vite 生产构建 | 通过 |
| npm 官方 registry 审计 | `0 vulnerabilities` |

前端覆盖率范围为 `src/demo/adapter.js` 与 `src/utils/**/*.js`。登录、个人资料弹层和路由守卫组件/行为测试单独计入 68 项测试，116 路由浏览器 E2E 继续覆盖真实渲染与网络行为；该报告不宣称整个 Vue 页面代码库达到 71.47%。

#### 2.1 GitHub Pages 浏览器演示

浏览器演示使用独立 `demo` 模式构建，不进入正式构建产物。验收覆盖：

- 6 个固定身份全部进入各自工作台，角色码与资料保持独立。
- 16 个后台页面和 7 个读者页面逐页巡检，Console 错误/警告、失败提示与横向溢出均为 0。
- 读者借阅使目标库存从 `3` 降至 `2`，归还后恢复；物流入库使目标库存从 `1` 增至 `5`，重复入库由单元测试验证不会再次加库存。
- “沿着书签”在 3 个收藏信号下进入内容推荐，关闭个性化后回到公共荐书；桌面和 390 × 844 移动视口均无整页横向溢出。
- 上传、下载、导出、邮件、注册和注销在演示模式中明确阻止，未知写操作不会返回伪成功。
- 390 × 844 移动视口无横向溢出；本地 Pages 产物共 128 个静态文件、89 个脚本，逐个 HTTP 请求均为 `200`，脚本 MIME 均正确。
- ECharts 公共核心与折线、柱状、饼图运行时分别延迟加载；登录和普通业务页不下载图表依赖，统计页按需加载 `466.89 KB`（gzip `158.09 KB`）公共核心及 `15.98-27.13 KB` 图型增量。

### 3. 六个固定权限身份真实全链路

系统使用 5 个角色码，固定验收身份为超级管理员、馆务协调员、普通管理员、读者、采购员和物流员。6 个身份均通过真实登录接口进入系统，当前完整流程记录 73 次真实 API 响应，覆盖：

- 读者查询、借阅、归还、续借、预约、收藏、可解释荐书与互动。
- 超级管理员权限调整和跨角色数据检查。
- 管理员创建采购需求。
- 采购员认领、推进并分配物流。
- 物流员更新运输、到馆和入库。
- 采购入库后库存从 `6` 增加到 `8`，重复入库未再次增加。

### 4. 并发与一致性

本轮三个后端实例共享同一个 MySQL、Redis 与 RabbitMQ，完成 8 个跨实例一致性场景、176 次真实请求。独立并发套件使用 20 名临时读者及采购、物流身份完成 20 个场景、393 次真实请求，覆盖库存竞争、重复借还、重复预约、注销竞态与采购状态竞争，没有出现破坏业务不变量的结果。

历史三批强并发基线仍保留：突发量 96、128、160，合计 1,986 次场景请求，最大场景 P95 461 ms。该数字用于证明更高突发下的历史正确性，本轮 176 次跨实例复核与 393 次并发回归用于证明 v1.2.3 最终代码状态。

重点验证包括：

- 库存为 1 时并发借阅只允许一个请求成功。
- 12 路并发注册共享同一规范化邮箱时恰好 3 个成功；资料换绑和物理删除同步移动或释放名额。
- 独立的三实例邮箱配额回归同时提交 100 个请求，结果为 3 个成功、97 个明确配额拒绝、0 个 HTTP 500 或其他失败；三个实例分别承担 34、33、33 个请求，最终复跑 P95 为 1,169 ms。
- `npm run test:e2e:registration-email` 通过真实注册、登录、资料换绑和物理删除接口验证：未证明邮箱控制权前只返回通用验证码错误，不泄露账号、署名或邮箱配额状态；验证后才返回精确冲突或配额结果，删除后名额可再次使用。
- 重复归还、重复预约取消和重复采购入库保持幂等。
- 用户资料、角色和业务状态更新不覆盖相邻事务。
- 通知任务使用原子认领、租约和重试，避免重复处理。
- 新增三实例边界回归完成 4 个场景、76 次真实请求：`20606` 签发的令牌在 `20607` 修改密码或角色后由 `20608` 拒绝；不存在账号、错误密码、冻结账号和禁用账号返回同一通用登录错误；采购员不能把“已下单”直接改为“已发货”；25 条消息按 20 + 5 分页时，未展示的 5 条保持未读。

专项回归最初真实发现了配额行 `INSERT IGNORE` 共享锁升级导致的 MySQL 死锁。修复后改为从第一条语句直接取得排他行锁，并把该场景固化为 `npm run test:e2e:email-quota`；最终数据库中的配额控制值与规范化邮箱实际账号数均为 3，InnoDB 未再记录死锁。

#### 4.1 v1.2.0 推荐闭环增量回归

推荐 E2E 使用三个后端实例共享同一 MySQL、Redis 和 RabbitMQ。先通过读者接口清空推荐派生记录，再把 8 个首次生成请求均衡发送到三个实例，最终只产生 1 个 `CONTENT` 批次，6 个返回条目及 `generatedAt` 完全一致。随后临时创建两名真实读者和一本候选图书，以共同收藏形成协同信号，三个实例都返回一致的 `HYBRID` 结果；临时数据在断言后完整清理。

- 三个收藏信号满足个性化阈值，已收藏图书不会出现在候选中，前三项作者保持多样。
- 关闭个性化后返回 `PUBLIC`，重新开启后恢复 `CONTENT`；共享收藏不足时不会伪造协同理由。
- 曝光、点击、推荐后收藏和“不感兴趣”四种归因均真实写入 MySQL；`INSERT IGNORE` 与唯一键保证重复事件原生幂等，不依赖异常控制流。
- “不感兴趣”后图书立即平滑移出前端列表，并在后续 30 天新批次中被过滤；收藏后来源版本变化，原条目同样立即从推荐中移除。
- Redis 可用时以全局/用户两级版本令牌避免每次聚合全部图书与行为；Redis 关闭或临时不可用时回退到 MySQL 聚合指纹，业务正确性不依赖缓存。
- 清除历史后批次、条目和事件为 0，收藏仍存在；测试结束后演示读者恢复原有 3 个收藏。
- 首轮真实测试发现新批次首次响应保留 JVM 纳秒、复用响应按 MySQL 秒精度返回的问题；实现统一为数据库秒精度后，在全新卷上复测通过。

### 5. 浏览器与 F12 等价诊断

浏览器自动诊断覆盖 116 个路由检查、456 次 API 响应和 6,206 次总网络响应：

其中页面运行阶段记录 438 次 API 响应和 6,188 次网络响应；进入页面前，6 个固定权限身份各完成验证码、登录和身份读取 3 次准备请求，共 18 次。两部分合计即 456 次 API 响应和 6,206 次总网络响应，自动报告会同时保留分项与汇总。

| 项目 | 结果 |
| --- | ---: |
| Console 错误与警告 | 0 |
| 页面异常 | 0 |
| 失败请求 | 0 |
| 网络错误 | 0 |
| 横向溢出路由 | 0 |

同时检查了桌面与移动视口、登录与 6 个固定权限身份路由、图表画布、动态依赖加载和主要交互页面。独立布局审查另覆盖 116 个页面和 21 个关键弹层，横向溢出与非预期元素越界均为 0。Vite 依赖预构建已固定，不再出现 `504 Outdated Optimize Dep`。

### 6. 数据库与中间件

- 34 条外键关系全部检查，孤儿记录为 0。
- 推荐批次、条目和事件的孤儿记录为 0，同一用户/条目/事件类型的重复记录为 0。
- 库存范围、活动借阅唯一性、活动预约唯一性、采购入库幂等和文件引用不变量违规均为 0。
- 邮箱配额表与用户数据一致，同一规范化邮箱的关联数不超过 3。
- `user.auth_version` 在 24 张表的 fresh 数据库中存在且全为非空；四类认证状态变更的旧令牌跨实例失效。
- Redis 通过真实缓存键与 TTL 验证。
- 同一来源 IP 的注册令牌桶额度轮流由 `20606`、`20607`、`20608` 消费，前 12 次进入业务校验，第 13 次统一返回 `429`，证明 Redis Lua 原子桶跨实例共享。
- 受信代理实例使用 `X-Forwarded-For` 形成共享安全键；不受信代理实例忽略伪造头，只记录真实直连地址。限流、登录保护和操作审计使用同一解析结果。
- 合法 `X-Request-ID` 在响应和错误日志 MDC 中保持一致；非法值被替换为 UUID，未进入日志上下文。生产 Nginx 返回 CSP、Referrer、Permissions、`nosniff` 和禁止嵌入响应头。
- Redis 故障恢复测试验证了待删除键会在恢复后的首次操作前重放，旧登录锁和旧验证码不会重新生效。
- RabbitMQ 故障恢复与队列消费通过；注入无法反序列化的通知消息后，主队列立即清空，消息进入 `dark.room.library.notification-task.dead`，没有无限重新入队。最终并发套件保留 2 条发送到虚构 `.local` 地址的可补偿通知任务，用于验证邮件失败后的租约、重试和恢复路径，不属于业务一致性违规。操作日志不是 RabbitMQ 消费结果，而是业务提交后通过独立事务写入的最佳努力审计。
- 通知任务以最大重试次数 1 触发真实终态后，数据库状态为 `4`，本地 Webhook 收到 Bearer 令牌、脱敏邮箱 `a***@example.com` 和截断错误，未收到邮件正文。
- 向通知主队列注入毒消息后，消息进入死信队列；三个实例同时轮询时仅一个实例通过 Redis 冷却键取得告警资格。本地 Webhook 实际收到 `rabbit_dead_letter_backlog`、队列名和消息数，不包含消息正文。清理后两个主队列和两个死信队列均为 0。
- 生产 Profile 首次使用已知 RabbitMQ 演示密码时按预期拒绝启动；切换隔离的数据库/RabbitMQ 凭据并配置 Webhook 后，实例在 `20609` 就绪。登录尝试 1 至 20 次进入业务校验，第 21 次起返回 `429`，证明收紧后的生产令牌桶生效。
- 后端无意外错误、内存溢出或死锁；未配置公开 SMTP 时保留预期的补偿重试警告，并在部署说明中明确真实收件仍需部署者验收。
- 前端运行时错误与警告均为 0。
- Compose 发布复核中，`/health/live`、`/health/ready`、超级管理员 `/health/details` 与 RabbitMQ 管理 API 均通过；fresh 数据库上的完整业务链路验证采购订单入库后库存 `6 -> 8`，五个服务按依赖顺序完整停启后再次执行六身份链路，库存继续从 `8 -> 10`，证明业务状态和上传文件并未因重启丢失。
- v1.2.0 增量复核中，五个 Compose 服务和两个附加后端实例均正常运行；原有三实例一致性套件完成 8 个场景、182 次请求，推荐专项完成 10 项检查，覆盖三实例首次生成、真实 `HYBRID` 协同、原生事件幂等和“不感兴趣”过滤。单实例并发补充完成 20 个场景、393 次请求；注册邮箱隐私闭环通过；三实例邮箱 100 请求结果为 3 次成功、97 次明确拒绝。七个容器重启次数均为 0，Redis AOF 写入正常，RabbitMQ 两个队列待消费和未确认消息均为 0。
- v1.2.2 冻结复核中，34 条外键逐条查询的孤儿记录均为 0；负库存、活动借阅重复、活动预约重复、推荐事件重复、采购库存标记和邮箱配额违规均为 0，InnoDB 没有最新死锁记录。Redis AOF 写入状态为 `ok`、拒绝连接和淘汰键为 0；RabbitMQ 两个主队列和两个毒消息队列的待消费、未确认消息均为 0，三个基础设施容器重启次数均为 0。
- v1.2.3 边界加固复核中，库存范围、活动借阅/预约重复、推荐事件重复、采购库存标记和邮箱配额违规均为 0，InnoDB 无最新死锁；Redis AOF 写入为 `ok`、拒绝连接和淘汰键为 0；四个 RabbitMQ 队列均无积压，九个验收容器重启和 OOM 均为 0。

真实 SMTP 邮件发送依赖外部邮箱授权码，不在公开源码的自动回归中保存凭据。邮件失败重试与补偿逻辑已由自动测试覆盖，部署者仍应在自己的邮箱环境完成一次人工收件验收。

### 7. 结论

本次验证证明当前公开基线在本地完整依赖环境中能够完成前端、后端、数据库和中间件的真实联动，并在并发、权限、状态机、浏览器运行与数据完整性方面保持一致。三实例测试只覆盖共享 MySQL 的业务一致性，不覆盖共享文件存储；当前本地文件目录仍是单机部署边界。上述延迟数据是正确性测试中的观测值，不等同于长期生产流量、跨地域容灾或无限规模保证。详细取舍见 [`architecture-review.md`](architecture-review.md)。
